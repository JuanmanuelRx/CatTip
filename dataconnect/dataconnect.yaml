specVersion: "v1"
serviceId: "cattip"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cattip-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
